import 'package:flutter/material.dart';

class CardColor{
  Color backgroundColor;
  Color foregroundColor;
  Color borderColor;

  CardColor(this.backgroundColor, this.foregroundColor,this.borderColor);
}